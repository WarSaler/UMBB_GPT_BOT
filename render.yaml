services:
  - type: web
    name: umbb-gpt-bot
    env: python
    plan: free
    buildCommand: |
      echo "🔧 Установка зависимостей:"
      pip install -r requirements.txt
      echo "✅ Проверка установленных пакетов:"
      pip list | grep -E "(loguru|python-dotenv)"
      echo "🧪 Тестирование HTTP сервера:"
      python3 -c "import bot; print('✅ HTTP сервер загружен без ошибок')"
      echo "🎉 Сборка завершена успешно!"
    startCommand: python3 bot.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: "10000"